"use strict"; //If use "use strict" before starting write code then it's treat all js code as newer version

// alert(3 + 3) //It's not working here because using Node.js, it's not browser

// This all are primitive dataType
let name = "PURE"; // String
let number = 20; // Number
let isLoggedIn = false; // Boolean
let state = null;

// Bigint (For use too long number)
// Null => Standalone Value
// Undefined => Value is not assigned to variable
// Symbol => Unique

console.table([typeof "Suvendu", typeof 10, typeof true, typeof undefined, typeof null]);
